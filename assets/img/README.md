Drop your profile photo here as `andrew.jpg` (or update the filename referenced
in `about.md` if you'd rather call it something else). That's the only image
this site currently expects you to supply manually — everything else uses
free stock photography.
